# 🚀 دليل البدء السريع - مكسبي

## خطوة بخطوة للمبتدئين

### الخطوة 1️⃣: تثبيت المكتبات

افتح PowerShell في مجلد المشروع واكتب:

```powershell
npm install
```

⏳ انتظر حتى ينتهي التثبيت (قد يستغرق 2-5 دقائق)

---

### الخطوة 2️⃣: تشغيل المشروع

بعد انتهاء التثبيت، اكتب:

```powershell
npm run dev
```

✅ عند نجاح التشغيل، سترى:

```
   ▲ Next.js 15.0.0
   - Local:        http://localhost:3000
   - Network:      http://192.168.x.x:3000

 ✓ Ready in 2.5s
```

---

### الخطوة 3️⃣: افتح المتصفح

افتح المتصفح واذهب إلى:

```
http://localhost:3000
```

🎉 ستُحوّل تلقائياً إلى صفحة تسجيل الدخول!

---

## 📱 الصفحات المتاحة

### بدون تسجيل دخول:
- 🔐 تسجيل الدخول: `http://localhost:3000/auth/login`
- 📝 إنشاء حساب: `http://localhost:3000/auth/register`

### بعد تسجيل الدخول:
- 📊 لوحة القيادة: `http://localhost:3000/dashboard`
- 📦 المنتجات: `http://localhost:3000/dashboard/products`
- 🛒 الطلبات: `http://localhost:3000/dashboard/orders`
- 👥 العملاء: `http://localhost:3000/dashboard/customers`
- 💰 المصروفات: `http://localhost:3000/dashboard/expenses`
- 📈 المخزون: `http://localhost:3000/dashboard/stock`
- 📑 التقارير: `http://localhost:3000/dashboard/reports`

---

## ⚙️ الإعدادات الضرورية

### ملف `.env.local`

تأكد من وجود هذا الملف مع المحتوى التالي:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_APP_NAME=مكسبي
NEXT_PUBLIC_CURRENCY=ج.م
```

📝 **ملاحظة**: غيّر `localhost:5000` إلى عنوان الـ Backend API الخاص بك

---

## 🔧 متطلبات Backend

يحتاج المشروع إلى .NET 8 Backend API يعمل على:
```
http://localhost:5000
```

تأكد من أن Backend يعمل قبل تشغيل Frontend!

---

## ❓ المشاكل الشائعة وحلولها

### 🔴 المشكلة: `npm: command not found`

**الحل**: ثبّت Node.js من:
```
https://nodejs.org
```
اختر LTS version (18 أو أحدث)

---

### 🔴 المشكلة: أخطاء أثناء `npm install`

**الحل**: احذف المجلد وحاول مرة أخرى:
```powershell
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
```

---

### 🔴 المشكلة: Port 3000 مستخدم

**الحل**: استخدم port آخر:
```powershell
$env:PORT=3001; npm run dev
```

---

### 🔴 المشكلة: API لا يستجيب

**الحل**: 
1. تحقق من Backend يعمل
2. تحقق من `.env.local` صحيح
3. تحقق من CORS في Backend

---

## 📞 كيف تختبر النظام؟

### 1. إنشاء حساب جديد
1. اذهب إلى `/auth/register`
2. املأ البيانات:
   - الاسم: أحمد محمد
   - اسم المتجر: متجري
   - البريد: test@test.com
   - كلمة المرور: 123456
3. اضغط "إنشاء حساب"

### 2. تسجيل الدخول
1. اذهب إلى `/auth/login`
2. استخدم البيانات السابقة
3. اضغط "تسجيل الدخول"

### 3. استكشف النظام
- جرب كل صفحة من الـ Sidebar
- اختبر البحث في المنتجات
- شاهد التقارير
- اكتشف الميزات!

---

## 🎨 التخصيص السريع

### تغيير اللون الأساسي

في `tailwind.config.js`:
```javascript
colors: {
  primary: {
    DEFAULT: "hsl(221.2 83.2% 53.3%)", // غير هذا
  }
}
```

### تغيير العملة

في `.env.local`:
```env
NEXT_PUBLIC_CURRENCY=$ # أو ريال أو أي عملة
```

### تغيير اسم التطبيق

في `.env.local`:
```env
NEXT_PUBLIC_APP_NAME=اسم متجرك
```

---

## 📚 الملفات المفيدة

- 📖 `README.md` - نظرة عامة ومعلومات أساسية
- 🔧 `INSTALLATION.md` - دليل التثبيت التفصيلي
- 📋 `PROJECT_SUMMARY.md` - ملخص شامل للمشروع
- 👨‍💻 `DEVELOPER_GUIDE.md` - دليل المطور الكامل

---

## ✅ جاهز للعمل!

بعد اتباع الخطوات أعلاه، نظامك جاهز 100%! 🎉

### الخطوات التالية:
1. ✅ تشغيل Backend API
2. ✅ تشغيل Frontend
3. ✅ إنشاء حساب
4. ✅ البدء في الاستخدام!

---

## 🆘 تحتاج مساعدة؟

راجع الملفات الأخرى للتفاصيل:
- مشاكل تقنية → `DEVELOPER_GUIDE.md`
- تثبيت متقدم → `INSTALLATION.md`
- فهم المشروع → `PROJECT_SUMMARY.md`

**بالتوفيق! 🚀**
